<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                  x:Class="Gruas_Atlas.Conf_Elimminar">

    <StackLayout Style="{StaticResource Popupform}">

        <Label Text=" "/>

        <Label Text="{StaticResource pregunta_eliminar}"
               Style="{StaticResource LabelPopUp}"
         />

        <Label Text=" "/>

        <Grid Padding="12">
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Text="{StaticResource aceptar}" 
                    Clicked="btnCompararID"
                    Grid.Row="0"
                    Grid.Column="0"
                    Style="{StaticResource ButtonPantallas}"
             />
            <Button Text="{StaticResource cancelar}" 
                    Clicked="btnClosePopup"
                    Grid.Row="0"
                    Grid.Column="1"
                    Style="{StaticResource ButtonPantallas}"
             />
        </Grid>
    </StackLayout>

</pages:PopupPage>
